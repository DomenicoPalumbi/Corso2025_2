<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %>
<!DOCTYPE html>
<html>
<head>
    <title>
        <c:choose>
            <c:when test="${not empty corso.id}">
                Modifica Corso
            </c:when>
            <c:otherwise>
                Nuovo Corso
            </c:otherwise>
        </c:choose>
    </title>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"/>
</head>
<body class="container mt-4">
<h1>
    <c:choose>
        <c:when test="${not empty docente.id}">
            Modifica Corso
        </c:when>
        <c:otherwise>
            Nuovo Corso
        </c:otherwise>
    </c:choose>
</h1>

<form action="<c:url value='${not empty corso.id ? "/corsi/" : "/corsi"}${not empty corso.id ? corso.id : ""}'/>" method="post">
    <div class="mb-3">
        <label for="nome" class="form-label">Nome</label>
        <input type="text" name="nome" id="nome" class="form-control" value="${corso.nome}" required>
    </div>
    <div class="mb-3">
        <label for="annoAccademico" class="form-label">annoAccademico</label>
        <input type="text" name="annoAccademico" id="annoAccademico" class="form-control" value="${corso.annoAccademico}" required>
    </div>
    <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input type="idDocente" name="idDocente" id="idDocente" class="form-control" value="${corso.idDocente}" required>
    </div>
    <button type="submit" class="btn btn-success">Salva</button>
    <a href="<c:url value='/corsi/lista'/>" class="btn btn-secondary">Annulla</a>
</form>
</body>
</html>
